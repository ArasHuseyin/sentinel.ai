# The published file set is controlled by "files" in package.json, which is a
# whole-directory allowlist (dist). This file is only a safety net for artifacts
# that could end up *inside* dist/ despite tsconfig.build.json excluding them.
dist/__tests__
